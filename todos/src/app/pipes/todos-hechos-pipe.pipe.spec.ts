import { TodosHechosPipePipe } from './todos-hechos-pipe.pipe';

describe('TodosHechosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TodosHechosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
