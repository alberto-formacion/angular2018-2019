import { PriorizarTodoPipePipe } from './priorizar-todo-pipe.pipe';

describe('PriorizarTodoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorizarTodoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
